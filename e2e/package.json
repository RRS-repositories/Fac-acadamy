{
  "name": "@fac-academy/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright end-to-end suite (grows in S10). Synthetic accounts only.",
  "scripts": {
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
