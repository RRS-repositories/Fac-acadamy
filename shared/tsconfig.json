{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // shared/ also runs in the browser, so no Node globals leak in here.
    "types": []
  },
  "include": ["src", "test"]
}
